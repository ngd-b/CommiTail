# Exclude source and config files
src/**
.vscode/**
test/**
*.ts
*.map

# Exclude build system files
# Exclude all node modules except runtime deps
node_modules/**
!node_modules/vscode-nls/**
tsconfig.json
package-lock.json
.vscodeignore

# Exclude local dev output
.gitignore
commitail.config.json

scripts

docs

# Keep only what's needed:
# - out/ (compiled code)
# - package.json
# - README.md
# - icons, if any
